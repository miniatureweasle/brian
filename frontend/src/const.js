export const HEIGHT = window.innerHeight;
export const WIDTH = window.innerWidth;

export const BUFFER = 30;

export const POSITIONMAXX = window.innerHeight - BUFFER;
export const POSITIONMAXY = window.innerWidth - BUFFER;